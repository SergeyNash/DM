# SARIF Parser - Requirements
# 
# Этот парсер не требует внешних зависимостей!
# Используется только стандартная библиотека Python 3.7+
#
# Минимальная версия Python: 3.7
# Рекомендуемая версия: 3.9+
#
# Опциональные зависимости для расширенных возможностей:
# 
# openpyxl>=3.0.0         # Для экспорта в Excel (опционально)
# matplotlib>=3.3.0       # Для графиков и визуализации (опционально)
# jinja2>=2.11.0          # Для продвинутых HTML шаблонов (опционально)
# flask>=2.0.0            # Для веб-интерфейса (опционально)
# flask-cors>=3.0.10      # Для CORS между статикой и API

flask>=2.3.0
flask-cors>=3.0.10
#
# Все опциональные зависимости можно установить при необходимости:
# pip install openpyxl matplotlib jinja2 flask

